# =============================================================================
# MAI Health Coach - local dev environment variables
#
# Usage (inside deploy/):
#   cp .env.example .env
#   # then edit .env if you want different local credentials
#
# .env is gitignored. .env.example is committed and documents every required
# variable. NEVER put real secrets in this file - local dev defaults only.
# =============================================================================

# -- PostgreSQL ---------------------------------------------------------------
# Used by both the postgres service and the API's ConnectionStrings__Default.
POSTGRES_USER=maihc
POSTGRES_PASSWORD=localdevpassword
POSTGRES_DB=maihealthcoach

# -- Future secrets (uncomment / add as features land) ------------------------
# CLERK_AUTHORITY=https://your-clerk-instance.clerk.accounts.dev
# ANTHROPIC__API_KEY=sk-ant-...
